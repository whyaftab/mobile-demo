import { Language } from '@localization/i18n';

export const AVAILABLE_LANGUAGE: { [key in Language]: { label: string } } = {
  English: {
    label: 'English',
  },
  French: {
    label: 'French',
  },
};
